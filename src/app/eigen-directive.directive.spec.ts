import { EigenDirectiveDirective } from './eigen-directive.directive';

describe('EigenDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EigenDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
